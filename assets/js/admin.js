/**
 * Admin JavaScript para o plugin Tutor Pro Charts
 */
(function($) {
    'use strict';

    // Quando o DOM estiver pronto
    $(document).ready(function() {
        // Código para inicialização no admin
    });

})(jQuery);